{
  "buildCommand": "NODE_OPTIONS='--max-old-space-size=4096 --gc-interval=100' npx prisma generate && next build",
  "framework": "nextjs",
  "installCommand": "npm install --legacy-peer-deps --ignore-scripts && npx prisma generate"
}
