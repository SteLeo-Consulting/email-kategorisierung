{
  "version": 2,
  "buildCommand": "cd apps/web && NODE_OPTIONS='--max-old-space-size=3584' npx prisma generate && npx next build",
  "installCommand": "npm install",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/process-emails",
      "schedule": "*/5 * * * *"
    }
  ]
}
